# dependencies — installed fresh on each machine (committed Windows copies lose Linux exec permissions)
node_modules/

# build output
dist/
*.tsbuildinfo

# local / editor
.vercel
.env*.local
.DS_Store
Thumbs.db
*.log
